'use strict';

const models = require("../../models/index");

const AccessTokenModel = models.AccessTokens;

const MongoAccessTokenDao = (() => {

   return {


   };
})();

module.exports = MongoAccessTokenDao;
